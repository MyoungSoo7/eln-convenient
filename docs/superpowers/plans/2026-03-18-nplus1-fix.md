# N+1 쿼리 문제 해결 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 전체 서비스에서 발견된 N+1 쿼리 패턴 7건을 제거하여 DB/HTTP 요청 횟수를 최소화한다.

**Architecture:** ELN 서비스에 배치 조회(`POST /api/notes/batch`)와 통계(`GET /api/notes/stats`) 엔드포인트를 추가하고, 각 서비스의 이중 쿼리와 루프 HTTP 호출 패턴을 단일 쿼리/배치 호출로 교체한다.

**Tech Stack:** TypeScript, Express, Fastify (api-gateway), Prisma, PostgreSQL, Jest (eln-service / signature-audit-service), Node.js http 모듈 (file-service elnClient)

**실행 순서 주의:** Task 1 (ELN 새 엔드포인트) → Task 4 (signature-audit) + Task 5 (dashboard) + Task 6 (file-service) 순서 필수. Task 2, Task 3은 독립적이므로 병렬 실행 가능.

---

## 변경 파일 맵

| 파일 | 역할 |
|------|------|
| `services/eln-service/src/controllers/note.controller.ts` | `getNoteStats`, `getNotesBatch` 핸들러 추가; `getAttachments`, `addAttachment`, `getTags` 수정 |
| `services/eln-service/src/routes/note.routes.ts` | `/notes/stats`, `/notes/batch` 라우트 등록 (`/notes/:id` 앞) |
| `services/eln-service/src/__tests__/note.controller.test.ts` | 기존 import 확장 + 새 테스트 추가 |
| `services/signature-audit-service/src/lib/eln.ts` | `fetchNoteStats()` + `NoteStats` 인터페이스 추가 |
| `services/signature-audit-service/src/controllers/signature.controller.ts` | `getComplianceStats` 교체 |
| `services/signature-audit-service/src/__tests__/signature.controller.test.ts` | 신규 테스트 파일 |
| `services/api-gateway/src/routes/dashboard.ts` | ELN 4회 호출 → 1회 교체, notes 조합 블록 수정 |
| `services/file-service/src/lib/elnClient.ts` | `postRequest<T>()`, `getNotesBatch()` 추가 |
| `services/file-service/src/processors/zipProcessor.ts` | 루프 → 배치 fetch 교체 |
| `services/inventory-service/src/controllers/inventory.controller.ts` | `getItemHistory` include 단일 쿼리화 |

---

## Task 1: ELN — `GET /api/notes/stats` + `POST /api/notes/batch` 엔드포인트 추가

**Files:**
- Modify: `services/eln-service/src/controllers/note.controller.ts`
- Modify: `services/eln-service/src/routes/note.routes.ts`
- Modify: `services/eln-service/src/__tests__/note.controller.test.ts`

### 1-1: 테스트 작성

- [ ] `services/eln-service/src/__tests__/note.controller.test.ts` **52번 줄**의 기존 import를 확장한다.
  새 import를 별도로 추가하지 말고, 기존 import에 합친다.

기존:
```typescript
import { deleteNote, changeNoteStatus, adminUnlockNote } from '../controllers/note.controller';
```

수정 후:
```typescript
import {
  deleteNote, changeNoteStatus, adminUnlockNote,
  getNoteStats, getNotesBatch,
} from '../controllers/note.controller';
```

- [ ] 기존 mock 객체(`mockNoteDb`)에 `groupBy` 추가. 파일 상단 mock 변수 선언부에서:

```typescript
const mockNoteDb = {
  findUnique: jest.fn(),
  findMany: jest.fn(),
  create: jest.fn(),
  update: jest.fn(),
  delete: jest.fn(),
  count: jest.fn(),
  groupBy: jest.fn(),   // ← 추가
};
```

- [ ] 파일 끝에 테스트 블록 추가:

```typescript
// ── getNoteStats ─────────────────────────────────────────────────────
describe('getNoteStats', () => {
  beforeEach(() => jest.clearAllMocks());

  it('상태별 카운트를 groupBy로 조합해 반환한다', async () => {
    mockNoteDb.groupBy.mockResolvedValue([
      { status: 'draft',       _count: { _all: 5 } },
      { status: 'in_progress', _count: { _all: 3 } },
      { status: 'signed',      _count: { _all: 8 } },
      // 'locked'는 없음 → 기본값 0으로 채워져야 함
    ]);
    const req = makeReq({ query: { type: 'note' } });
    const { res, json } = makeRes();
    await getNoteStats(req, res);
    expect(json).toHaveBeenCalledWith({
      ok: true,
      data: { draft: 5, in_progress: 3, locked: 0, signed: 8, total: 16 },
    });
  });

  it('type이 잘못되면 400을 반환한다', async () => {
    const req = makeReq({ query: { type: 'invalid' } });
    const { res, status, json } = makeRes();
    await getNoteStats(req, res);
    expect(status).toHaveBeenCalledWith(400);
    expect(json).toHaveBeenCalledWith(expect.objectContaining({ ok: false }));
  });
});

// ── getNotesBatch ─────────────────────────────────────────────────────
describe('getNotesBatch', () => {
  beforeEach(() => jest.clearAllMocks());

  it('ID 배열로 노트 목록을 반환한다', async () => {
    const notes = [{ id: 'a' }, { id: 'b' }];
    mockNoteDb.findMany.mockResolvedValue(notes);
    const req = makeReq({ body: { ids: ['a', 'b'] } });
    const { res, json } = makeRes();
    await getNotesBatch(req, res);
    expect(json).toHaveBeenCalledWith({ ok: true, data: notes });
  });

  it('ids가 빈 배열이면 DB 호출 없이 빈 배열을 반환한다', async () => {
    const req = makeReq({ body: { ids: [] } });
    const { res, json } = makeRes();
    await getNotesBatch(req, res);
    expect(json).toHaveBeenCalledWith({ ok: true, data: [] });
    expect(mockNoteDb.findMany).not.toHaveBeenCalled();
  });

  it('ids가 없으면 400을 반환한다', async () => {
    const req = makeReq({ body: {} });
    const { res, status, json } = makeRes();
    await getNotesBatch(req, res);
    expect(status).toHaveBeenCalledWith(400);
    expect(json).toHaveBeenCalledWith(expect.objectContaining({ ok: false }));
  });

  it('ids가 500개 초과면 400을 반환한다', async () => {
    const req = makeReq({ body: { ids: Array(501).fill('uuid') } });
    const { res, status, json } = makeRes();
    await getNotesBatch(req, res);
    expect(status).toHaveBeenCalledWith(400);
  });
});
```

- [ ] 테스트가 실패하는지 확인한다.

```bash
cd services/eln-service && npm test -- --testPathPattern=note.controller
```

Expected: 두 가지 실패 패턴이 모두 발생해야 한다:
- `getNoteStats is not a function` (함수가 없으므로 모든 케이스 실패)
- type 검증 케이스도 함수가 없어 동일하게 실패. 구현 후 검증 로직이 없으면 "expected status(400) to have been called" 형태로 실패한다.

### 1-2: `getNoteStats` 핸들러 구현

- [ ] `services/eln-service/src/controllers/note.controller.ts` 맨 끝에 추가:

```typescript
/** GET /api/notes/stats */
export async function getNoteStats(req: Request, res: Response): Promise<void> {
  const type = (req.query.type as string) || 'note';
  if (!['note', 'protocol'].includes(type)) {
    res.status(400).json({ ok: false, error: 'type은 note 또는 protocol이어야 합니다.' });
    return;
  }
  try {
    const rows = await prisma.note.groupBy({
      by: ['status'],
      where: { type: type as NoteType, deletedAt: null },
      _count: { _all: true },
    });
    const base = { draft: 0, in_progress: 0, locked: 0, signed: 0 };
    for (const row of rows) {
      base[row.status as keyof typeof base] = row._count._all;
    }
    const total = base.draft + base.in_progress + base.locked + base.signed;
    res.json({ ok: true, data: { ...base, total } });
  } catch (err) {
    console.error('[getNoteStats]', err);
    res.status(500).json({ ok: false, error: '통계 조회 중 오류가 발생했습니다.' });
  }
}
```

### 1-3: `getNotesBatch` 핸들러 구현

- [ ] 같은 파일 끝에 이어서 추가:

```typescript
/** POST /api/notes/batch */
export async function getNotesBatch(req: Request, res: Response): Promise<void> {
  const { ids } = req.body;
  if (!Array.isArray(ids)) {
    res.status(400).json({ ok: false, error: 'ids는 문자열 배열이어야 합니다.' });
    return;
  }
  if (ids.length === 0) {
    res.json({ ok: true, data: [] });
    return;
  }
  if (ids.length > 500) {
    res.status(400).json({ ok: false, error: 'ids는 최대 500개까지 허용됩니다.' });
    return;
  }
  try {
    const notes = await prisma.note.findMany({
      where: { id: { in: ids }, deletedAt: null },
    });
    res.json({ ok: true, data: notes });
  } catch (err) {
    console.error('[getNotesBatch]', err);
    res.status(500).json({ ok: false, error: '노트 일괄 조회 중 오류가 발생했습니다.' });
  }
}
```

### 1-4: 테스트 통과 확인

- [ ] 테스트를 다시 실행한다.

```bash
cd services/eln-service && npm test -- --testPathPattern=note.controller
```

Expected: 추가한 테스트가 모두 PASS.

### 1-5: 라우트 등록

- [ ] `services/eln-service/src/routes/note.routes.ts`에서 **14번 줄** `router.post('/notes', ...)` 바로 아래, **15번 줄** `router.get('/notes/:id', ...)` 바로 위에 두 라우트를 삽입한다.

```typescript
// ─── N+1 최적화: GET /notes/stats는 반드시 /notes/:id 앞에 위치해야 한다.
// Express는 등록 순서대로 매칭하므로 /notes/:id가 먼저 있으면 'stats'가 id=stats로 처리된다.
// POST /notes/batch는 메서드가 달라 충돌이 없지만 관련 라우트로서 함께 배치한다.
router.get('/notes/stats',  requirePermission('note:read'),  ctrl.getNoteStats);
router.post('/notes/batch', requirePermission('note:read'),  ctrl.getNotesBatch);
```

삽입 후 순서 확인:
```
router.get('/notes',        requirePermission('note:read'),    ctrl.getNotes);
router.post('/notes',       requirePermission('note:write'),   ctrl.createNote);
router.get('/notes/stats',  requirePermission('note:read'),    ctrl.getNoteStats);   // ← 신규
router.post('/notes/batch', requirePermission('note:read'),    ctrl.getNotesBatch);  // ← 신규
router.get('/notes/:id',    requirePermission('note:read'),    ctrl.getNoteById);    // 기존
```

### 1-6: 빌드 확인 및 커밋

- [ ] TypeScript 컴파일 오류가 없는지 확인한다.

```bash
cd services/eln-service && npx tsc --noEmit
```

Expected: 오류 없음.

- [ ] 커밋한다.

```bash
git add services/eln-service/src/controllers/note.controller.ts \
        services/eln-service/src/routes/note.routes.ts \
        services/eln-service/src/__tests__/note.controller.test.ts
git commit -m "feat(eln-service): add GET /notes/stats and POST /notes/batch endpoints"
```

---

## Task 2: ELN — `getAttachments`, `addAttachment`, `getTags` 이중 쿼리 수정

**Files:**
- Modify: `services/eln-service/src/controllers/note.controller.ts`
- Modify: `services/eln-service/src/__tests__/note.controller.test.ts`

### 2-1: 테스트 작성

- [ ] 기존 import 줄에 `getAttachments`, `addAttachment`, `getTags`를 추가한다 (Task 1에서 이미 확장된 import에 합친다):

```typescript
import {
  deleteNote, changeNoteStatus, adminUnlockNote,
  getNoteStats, getNotesBatch,
  getAttachments, addAttachment, getTags,
} from '../controllers/note.controller';
```

- [ ] 파일 상단 mock 변수 선언부(기존 `mockNoteDb` 근처)에 두 변수를 추가한다:

```typescript
// 기존 mockNoteDb, mockNoteStatusHistoryDb, mockNoteRevisionDb 선언 바로 아래에 추가
const mockAttachmentDb = { create: jest.fn() };
const mockQueryRaw = jest.fn();
```

- [ ] `jest.mock('../lib/prisma', ...)` 팩토리에서 `attachment` 와 `$queryRaw`를 위 변수로 연결한다:

```typescript
jest.mock('../lib/prisma', () => ({
  __esModule: true,
  default: {
    note: mockNoteDb,
    noteStatusHistory: mockNoteStatusHistoryDb,
    noteRevision: mockNoteRevisionDb,
    noteLink: { create: jest.fn() },
    attachment: mockAttachmentDb,   // ← 변수로 교체
    template: { update: jest.fn() },
    $queryRaw: mockQueryRaw,        // ← 추가
  },
}));
```

- [ ] 파일 끝에 테스트 블록 추가:

```typescript
// ── getAttachments ─────────────────────────────────────────────────
describe('getAttachments', () => {
  beforeEach(() => jest.clearAllMocks());

  it('노트가 없으면 404를 반환한다', async () => {
    mockNoteDb.findUnique.mockResolvedValue(null);
    const req = makeReq({ params: { id: 'note-1' } });
    const { res, status, json } = makeRes();
    await getAttachments(req, res);
    expect(status).toHaveBeenCalledWith(404);
    expect(json).toHaveBeenCalledWith(expect.objectContaining({ ok: false }));
  });

  it('include로 첨부파일을 단일 쿼리로 반환한다', async () => {
    const attachments = [{ id: 'att-1', fileName: 'a.pdf' }];
    // 수정된 구현은 findUnique with include 1회만 호출한다.
    // 기존 구현(findNote + findMany)과 달리 findUnique에 attachments가 포함되어 반환된다.
    mockNoteDb.findUnique.mockResolvedValue({ id: 'note-1', attachments });
    const req = makeReq({ params: { id: 'note-1' } });
    const { res, json } = makeRes();
    await getAttachments(req, res);
    expect(json).toHaveBeenCalledWith({ ok: true, data: attachments });
    // findUnique 1회만 호출됐는지 확인 (이중 쿼리 없음)
    expect(mockNoteDb.findUnique).toHaveBeenCalledTimes(1);
    expect(mockNoteDb.findUnique).toHaveBeenCalledWith(
      expect.objectContaining({ include: expect.objectContaining({ attachments: expect.anything() }) }),
    );
  });
});

// ── addAttachment ──────────────────────────────────────────────────
describe('addAttachment', () => {
  beforeEach(() => jest.clearAllMocks());

  it('첨부파일을 201로 생성한다', async () => {
    const created = { id: 'att-1', noteId: 'note-1', fileName: 'a.pdf' };
    mockAttachmentDb.create.mockResolvedValue(created);
    const req = makeReq({
      params: { id: 'note-1' },
      body: { fileId: 'f1', fileName: 'a.pdf' },
      headers: { 'x-user-id': 'u1', 'x-user-role': 'researcher' },
    });
    const { res, status, json } = makeRes();
    await addAttachment(req, res);
    expect(status).toHaveBeenCalledWith(201);
    expect(json).toHaveBeenCalledWith({ ok: true, data: created });
    // findNote가 호출되지 않았는지 확인 (존재확인 쿼리 없음)
    expect(mockNoteDb.findUnique).not.toHaveBeenCalled();
  });

  it('noteId FK 위반(P2003)이면 404를 반환한다', async () => {
    const fkErr = Object.assign(new Error('FK violation'), { code: 'P2003' });
    mockAttachmentDb.create.mockRejectedValue(fkErr);
    const req = makeReq({
      params: { id: 'ghost-note' },
      body: { fileId: 'f1', fileName: 'a.pdf' },
      headers: { 'x-user-id': 'u1', 'x-user-role': 'researcher' },
    });
    const { res, status, json } = makeRes();
    await addAttachment(req, res);
    expect(status).toHaveBeenCalledWith(404);
    expect(json).toHaveBeenCalledWith(expect.objectContaining({ ok: false }));
    // findNote(findUnique)가 호출되지 않았는지 확인 (존재확인 쿼리 제거됨)
    expect(mockNoteDb.findUnique).not.toHaveBeenCalled();
  });
});

// ── getTags ───────────────────────────────────────────────────────
describe('getTags', () => {
  beforeEach(() => jest.clearAllMocks());

  it('$queryRaw UNNEST로 중복 없는 태그 목록을 반환한다', async () => {
    mockQueryRaw.mockResolvedValue([{ tag: 'alpha' }, { tag: 'beta' }]);
    const req = makeReq({ query: { type: 'note' } });
    const { res, json } = makeRes();
    await getTags(req, res);
    expect(json).toHaveBeenCalledWith({ ok: true, data: ['alpha', 'beta'] });
    // findMany를 사용하지 않음 (전체 테이블 스캔 없음)
    expect(mockNoteDb.findMany).not.toHaveBeenCalled();
    expect(mockQueryRaw).toHaveBeenCalledTimes(1);
  });

  it('DB에서 온 순서 그대로 반환한다 (ORDER BY는 SQL에서 처리됨)', async () => {
    // 구현이 JS에서 추가 정렬을 하지 않고 DB ORDER BY에 의존함을 검증.
    // 모의 데이터를 역순으로 주면 그대로 역순 반환해야 한다.
    mockQueryRaw.mockResolvedValue([{ tag: 'zeta' }, { tag: 'alpha' }]);
    const req = makeReq({ query: { type: 'note' } });
    const { res, json } = makeRes();
    await getTags(req, res);
    // JS sort 없이 그대로 통과: ['zeta', 'alpha'] 그대로
    expect(json).toHaveBeenCalledWith({ ok: true, data: ['zeta', 'alpha'] });
  });

  it('type 쿼리 파라미터를 $queryRaw에 전달한다', async () => {
    mockQueryRaw.mockResolvedValue([]);
    const req = makeReq({ query: { type: 'protocol' } });
    const { res } = makeRes();
    await getTags(req, res);
    // $queryRaw tagged template literal의 첫 번째 인수는 string[] (TemplateStringsArray).
    // 두 번째 인수가 type 변수('protocol')임을 확인한다.
    const callArgs = mockQueryRaw.mock.calls[0];
    expect(callArgs[1]).toBe('protocol');
  });
});
```

**테스트 실패 이유 설명:**
- `getAttachments` 테스트: 기존 구현은 `findNote`(findUnique) + `attachment.findMany` 2회 호출한다. 테스트는 `findUnique`가 `include`와 함께 호출되고 첨부파일이 반환되길 기대하지만, 기존 구현은 `include` 없이 note만 조회한 뒤 별도 `findMany`를 호출하므로 반환값이 다르다.
- `addAttachment` 테스트: 기존 구현은 먼저 `findNote`로 존재확인 후 create하므로 `findUnique`가 1회 호출된다. 테스트는 `findUnique`가 호출되지 않아야 한다고 단언하므로 FAIL.
- `getTags` 테스트: 기존 구현은 `findMany`를 호출하지만, 테스트는 `findMany` 호출 없음을 단언하므로 FAIL.

- [ ] 테스트가 실패하는지 확인한다.

```bash
cd services/eln-service && npm test -- --testPathPattern=note.controller
```

Expected: `getAttachments`, `addAttachment`, `getTags` 관련 테스트 FAIL.

### 2-2: `getAttachments` 수정

- [ ] `services/eln-service/src/controllers/note.controller.ts`의 `getAttachments` 함수를 수정한다 (515번 줄 근방).

기존:
```typescript
export async function getAttachments(req: Request, res: Response): Promise<void> {
  try {
    const note = await findNote(req.params.id);
    if (!note) { res.status(404).json({ ok: false, error: '노트를 찾을 수 없습니다.' }); return; }
    const attachments = await prisma.attachment.findMany({
      where: { noteId: req.params.id },
      orderBy: { createdAt: 'asc' },
    });
    res.json({ ok: true, data: attachments });
```

수정 후:
```typescript
export async function getAttachments(req: Request, res: Response): Promise<void> {
  try {
    const note = await prisma.note.findUnique({
      where: { id: req.params.id },
      include: { attachments: { orderBy: { createdAt: 'asc' } } },
    });
    if (!note) { res.status(404).json({ ok: false, error: '노트를 찾을 수 없습니다.' }); return; }
    res.json({ ok: true, data: note.attachments });
```

### 2-3: `addAttachment` 수정

- [ ] 같은 파일의 `addAttachment`에서 `findNote` 호출을 제거하고 P2003 핸들링을 추가한다 (530번 줄 근방).

기존:
```typescript
export async function addAttachment(req: Request, res: Response): Promise<void> {
  try {
    const note = await findNote(req.params.id);
    if (!note) { res.status(404).json({ ok: false, error: '노트를 찾을 수 없습니다.' }); return; }

    const attachment = await prisma.attachment.create({
      data: {
        id: uuidv4(),
        noteId: req.params.id,
        fileId: req.body.fileId || uuidv4(),
        fileName: req.body.fileName || 'unknown',
        mimeType: req.body.mimeType || null,
        sizeBytes: req.body.sizeBytes || null,
        uploadedBy: (req.headers['x-user-id'] as string) || 'anonymous',
      },
    });
    res.status(201).json({ ok: true, data: attachment });
  } catch (err) {
    console.error('[addAttachment]', err);
    res.status(500).json({ ok: false, error: '첨부파일 등록 중 오류가 발생했습니다.' });
  }
}
```

수정 후:
```typescript
export async function addAttachment(req: Request, res: Response): Promise<void> {
  try {
    const attachment = await prisma.attachment.create({
      data: {
        id: uuidv4(),
        noteId: req.params.id,
        fileId: req.body.fileId || uuidv4(),
        fileName: req.body.fileName || 'unknown',
        mimeType: req.body.mimeType || null,
        sizeBytes: req.body.sizeBytes || null,
        uploadedBy: (req.headers['x-user-id'] as string) || 'anonymous',
      },
    });
    res.status(201).json({ ok: true, data: attachment });
  } catch (err: any) {
    // P2003: noteId FK 위반 → 노트가 존재하지 않음 (Attachment.noteId만 FK; fileId는 plain String)
    if (err?.code === 'P2003') {
      res.status(404).json({ ok: false, error: '노트를 찾을 수 없습니다.' });
      return;
    }
    console.error('[addAttachment]', err);
    res.status(500).json({ ok: false, error: '첨부파일 등록 중 오류가 발생했습니다.' });
  }
}
```

### 2-4: `getTags` 수정

- [ ] 같은 파일의 `getTags` 함수를 수정한다 (629번 줄 근방).

기존:
```typescript
export async function getTags(req: Request, res: Response): Promise<void> {
  const type = (req.query.type as NoteType) || 'note';
  try {
    const notes = await prisma.note.findMany({
      where: { type },
      select: { tags: true },
    });
    const tagSet = new Set<string>();
    notes.forEach((n) => n.tags.forEach((t) => tagSet.add(t)));
    res.json({ ok: true, data: Array.from(tagSet).sort() });
  } catch (err) {
```

수정 후:
```typescript
export async function getTags(req: Request, res: Response): Promise<void> {
  const type = (req.query.type as NoteType) || 'note';
  try {
    // UNNEST+DISTINCT로 DB 레벨에서 중복 제거 및 정렬.
    // 의도적 동작 변경: deletedAt IS NULL 필터 추가 → 소프트 삭제된 노트의 태그 제외.
    const rows = await prisma.$queryRaw<{ tag: string }[]>`
      SELECT DISTINCT UNNEST(tags) AS tag
      FROM "Note"
      WHERE type = ${type}::text AND "deletedAt" IS NULL
      ORDER BY tag
    `;
    res.json({ ok: true, data: rows.map((r) => r.tag) });
  } catch (err) {
```

### 2-5: 테스트 통과 확인 및 커밋

- [ ] 테스트를 다시 실행한다.

```bash
cd services/eln-service && npm test -- --testPathPattern=note.controller
```

Expected: 전체 PASS.

- [ ] 빌드 확인 후 커밋한다.

```bash
cd services/eln-service && npx tsc --noEmit
git add services/eln-service/src/controllers/note.controller.ts \
        services/eln-service/src/__tests__/note.controller.test.ts
git commit -m "fix(eln-service): remove double queries in getAttachments, addAttachment, getTags"
```

---

## Task 3: inventory-service — `getItemHistory` 이중 쿼리 수정

**Files:**
- Modify: `services/inventory-service/src/controllers/inventory.controller.ts`

> **TDD 예외 사항:** inventory-service에는 Jest가 설정되어 있지 않다. Jest 세업 없이 단위 테스트를 작성하면 팀 CI 환경에 사이드 이펙트가 생기므로, 이 Task는 예외적으로 tsc 빌드 확인으로 대체한다. 변경 범위가 `findUnique` 호출 1개를 `include` 방식으로 교체하는 것뿐이므로 타입 오류 부재가 충분한 안전망이 된다.

### 3-1: `getItemHistory` 수정

- [ ] `services/inventory-service/src/controllers/inventory.controller.ts`의 `getItemHistory` 함수를 수정한다 (316번 줄 근방).

기존:
```typescript
export async function getItemHistory(req: Request, res: Response): Promise<void> {
  try {
    const item = await prisma.inventoryItem.findUnique({ where: { id: req.params.id } });
    if (!item) { res.status(404).json({ ok: false, error: '아이템을 찾을 수 없습니다.' }); return; }

    const history = await prisma.inventoryHistory.findMany({
      where: { itemId: req.params.id },
      orderBy: { createdAt: 'desc' },
      take: 100,
    });

    res.json({ ok: true, data: history, total: history.length });
```

수정 후:
```typescript
export async function getItemHistory(req: Request, res: Response): Promise<void> {
  try {
    const item = await prisma.inventoryItem.findUnique({
      where: { id: req.params.id },
      include: { history: { orderBy: { createdAt: 'desc' }, take: 100 } },
    });
    if (!item) { res.status(404).json({ ok: false, error: '아이템을 찾을 수 없습니다.' }); return; }

    res.json({ ok: true, data: item.history, total: item.history.length });
```

### 3-2: 빌드 확인 및 커밋

- [ ] 컴파일 오류 없는지 확인한다.

```bash
cd services/inventory-service && npx tsc --noEmit
```

Expected: 오류 없음.

- [ ] 커밋한다.

```bash
git add services/inventory-service/src/controllers/inventory.controller.ts
git commit -m "fix(inventory-service): getItemHistory single query with include"
```

---

## Task 4: signature-audit-service — `fetchNoteStats` + `getComplianceStats` 교체

> **Task 1 완료 후 진행.** ELN `GET /api/notes/stats` 엔드포인트가 있어야 한다.

**Files:**
- Modify: `services/signature-audit-service/src/lib/eln.ts`
- Modify: `services/signature-audit-service/src/controllers/signature.controller.ts`
- Create: `services/signature-audit-service/src/__tests__/signature.controller.test.ts`

### 4-1: 테스트 작성

- [ ] `services/signature-audit-service/src/__tests__/signature.controller.test.ts`를 새로 생성한다.

```typescript
/**
 * signature.controller — getComplianceStats 단위 테스트
 */

// ── mock 변수 선언 ──────────────────────────────────────────────────
const mockPrismaSignature = { count: jest.fn() };
const mockFetchNoteStats = jest.fn();

// ── 모듈 모킹 ──────────────────────────────────────────────────────
jest.mock('../lib/prisma', () => ({
  __esModule: true,
  default: { signature: mockPrismaSignature },
}));

// MockElnServiceError를 jest.fn()이 아닌 실제 클래스로 선언하는 이유:
// getComplianceStats 내부에서 `err instanceof ElnServiceError` 체크를 한다.
// jest.mock이 모듈의 ElnServiceError를 MockElnServiceError로 교체하므로,
// 컨트롤러가 참조하는 ElnServiceError === MockElnServiceError가 된다.
// 따라서 `new MockElnServiceError(...)` 인스턴스는 instanceof 체크를 통과한다.
class MockElnServiceError extends Error {
  constructor(msg: string) { super(msg); this.name = 'ElnServiceError'; }
}

jest.mock('../lib/eln', () => ({
  fetchNoteStats: mockFetchNoteStats,
  fetchNotes: jest.fn(),
  fetchNote: jest.fn(),
  fetchNoteCount: jest.fn(),
  ElnServiceError: MockElnServiceError,
}));

// ── 컨트롤러 import (모킹 이후) ────────────────────────────────────
import type { Request, Response } from 'express';
import { getComplianceStats } from '../controllers/signature.controller';

// ── 헬퍼 ──────────────────────────────────────────────────────────
function makeReq(): Request {
  return { headers: {}, query: {}, params: {}, body: {}, ip: '127.0.0.1' } as unknown as Request;
}
function makeRes() {
  const json = jest.fn();
  const status = jest.fn().mockReturnValue({ json });
  return { res: { json, status } as unknown as Response, json, status };
}

// ── 테스트 ────────────────────────────────────────────────────────
describe('getComplianceStats', () => {
  beforeEach(() => jest.clearAllMocks());

  it('fetchNoteStats 1회 호출로 통계를 반환하며 in_progress를 pending으로 매핑한다', async () => {
    mockFetchNoteStats.mockResolvedValue({
      draft: 2, in_progress: 3, locked: 1, signed: 5, total: 11,
    });
    mockPrismaSignature.count.mockResolvedValue(5);
    const { res, json } = makeRes();
    await getComplianceStats(makeReq(), res);

    // fetchNoteStats가 1회만 호출됐는지 확인 (기존 4회 → 1회)
    expect(mockFetchNoteStats).toHaveBeenCalledTimes(1);
    expect(json).toHaveBeenCalledWith({
      ok: true,
      data: {
        signed: 5,
        pending: 3,   // in_progress → pending 키 매핑 확인
        locked: 1,
        draft: 2,
        totalSignatures: 5,
      },
    });
  });

  it('fetchNoteStats가 ElnServiceError를 던지면 503을 반환한다', async () => {
    mockFetchNoteStats.mockRejectedValue(new MockElnServiceError('ELN 연결 실패'));
    mockPrismaSignature.count.mockResolvedValue(0);
    const { res, status, json } = makeRes();
    await getComplianceStats(makeReq(), res);
    expect(status).toHaveBeenCalledWith(503);
    expect(json).toHaveBeenCalledWith(expect.objectContaining({ ok: false }));
  });
});
```

- [ ] 테스트가 실패하는지 확인한다.

```bash
cd services/signature-audit-service && npm test -- --testPathPattern=signature.controller
```

Expected: `fetchNoteStats is not a function` 또는 `fetchNoteStats` 호출 횟수 불일치로 FAIL.

### 4-2: `fetchNoteStats` 추가

- [ ] `services/signature-audit-service/src/lib/eln.ts` 파일 끝에 추가한다:

```typescript
export interface NoteStats {
  draft: number;
  in_progress: number;
  locked: number;
  signed: number;
  total: number;
}

/** GET /api/notes/stats → 상태별 카운트를 단 1회 HTTP 호출로 조회 (4회 fetchNoteCount 대체) */
export async function fetchNoteStats(type = 'note'): Promise<NoteStats> {
  const url = `${ELN_URL}/api/notes/stats?type=${type}`;
  const res = await fetchWithTimeout(url, { headers: internalHeaders() });
  if (!res.ok) throw new ElnServiceError(`eln-service 오류: ${res.status}`);
  const body = await res.json() as { ok: boolean; data: NoteStats };
  return body.data;
}
```

### 4-3: `getComplianceStats` 교체

- [ ] `services/signature-audit-service/src/controllers/signature.controller.ts`의 import에 `fetchNoteStats` 추가:

```typescript
import { fetchNoteCount, fetchNotes, fetchNote, fetchNoteStats, ElnServiceError } from '../lib/eln';
```

- [ ] `getComplianceStats` 함수 내부를 교체한다 (261–283번 줄).

기존:
```typescript
const [signed, pending, locked, draft, totalSignatures] = await Promise.all([
  fetchNoteCount('signed'),
  fetchNoteCount('in_progress'),
  fetchNoteCount('locked'),
  fetchNoteCount('draft'),
  prisma.signature.count({ where: { status: 'valid' } }),
]);

res.json({
  ok: true,
  data: { signed, pending, locked, draft, totalSignatures },
});
```

수정 후:
```typescript
const [stats, totalSignatures] = await Promise.all([
  fetchNoteStats('note'),
  prisma.signature.count({ where: { status: 'valid' } }),
]);

res.json({
  ok: true,
  data: {
    signed: stats.signed,
    pending: stats.in_progress,  // DB enum 'in_progress' → 응답 키 'pending' 유지 (기존 API 호환)
    locked: stats.locked,
    draft: stats.draft,
    totalSignatures,
  },
});
```

### 4-4: 테스트 통과 확인 및 커밋

- [ ] 테스트를 실행한다.

```bash
cd services/signature-audit-service && npm test -- --testPathPattern=signature.controller
```

Expected: 2개 테스트 모두 PASS.

- [ ] 빌드 확인 후 커밋한다.

```bash
cd services/signature-audit-service && npx tsc --noEmit
git add services/signature-audit-service/src/lib/eln.ts \
        services/signature-audit-service/src/controllers/signature.controller.ts \
        services/signature-audit-service/src/__tests__/signature.controller.test.ts
git commit -m "fix(signature-audit): replace 4x HTTP note count calls with single fetchNoteStats"
```

---

## Task 5: api-gateway — dashboard ELN 4회 호출 → 1회 교체

> **Task 1 완료 후 진행.**

**Files:**
- Modify: `services/api-gateway/src/routes/dashboard.ts`

> **Note:** api-gateway에는 Jest가 설정되어 있지 않다. tsc 빌드 확인으로 대체한다.

### 5-1: dashboard.ts 수정

- [ ] `services/api-gateway/src/routes/dashboard.ts`의 `Promise.allSettled` 블록에서 ELN 4회 호출을 1회로 교체한다 (46–53번 줄).

기존:
```typescript
// 1) 노트 상태별 카운트 (status별 4번 호출)
Promise.all([
  safeGet<any>(`${ELN_URL}/api/notes?type=note&status=draft&limit=1`, internalHeaders),
  safeGet<any>(`${ELN_URL}/api/notes?type=note&status=in_progress&limit=1`, internalHeaders),
  safeGet<any>(`${ELN_URL}/api/notes?type=note&status=signed&limit=1`, internalHeaders),
  safeGet<any>(`${ELN_URL}/api/notes?type=note&status=locked&limit=1`, internalHeaders),
]),
```

수정 후:
```typescript
// 1) 노트 상태별 통계 (GET /notes/stats 단일 호출)
safeGet<any>(`${ELN_URL}/api/notes/stats?type=note`, internalHeaders),
```

- [ ] notes 조합 블록을 교체한다 (66–77번 줄):

기존:
```typescript
let notes: Record<string, number | null> = { draft: null, in_progress: null, signed: null, locked: null, total: null };
if (noteStats.status === 'fulfilled' && noteStats.value) {
  const [draft, inProgress, signed, locked] = noteStats.value;
  notes = {
    draft:       draft?.total        ?? null,
    in_progress: inProgress?.total   ?? null,
    signed:      signed?.total       ?? null,
    locked:      locked?.total       ?? null,
    total: (draft?.total ?? 0) + (inProgress?.total ?? 0) + (signed?.total ?? 0) + (locked?.total ?? 0),
  };
}
```

수정 후:
```typescript
// safeGet이 body.data를 unwrap하므로 noteStats.value가 바로 stats 플랫 객체
let notes: Record<string, number | null> = { draft: null, in_progress: null, signed: null, locked: null, total: null };
if (noteStats.status === 'fulfilled' && noteStats.value) {
  const s = noteStats.value as { draft: number; in_progress: number; locked: number; signed: number; total: number };
  notes = { draft: s.draft, in_progress: s.in_progress, signed: s.signed, locked: s.locked, total: s.total };
}
```

### 5-2: 빌드 확인 및 커밋

- [ ] 컴파일 오류 없는지 확인한다.

```bash
cd services/api-gateway && npx tsc --noEmit
```

Expected: 오류 없음.

- [ ] 커밋한다.

```bash
git add services/api-gateway/src/routes/dashboard.ts
git commit -m "fix(api-gateway): replace 4x ELN note count calls with single GET /notes/stats"
```

---

## Task 6: file-service — `postRequest` + `getNotesBatch` + `zipProcessor` 배치 교체

> **Task 1 완료 후 진행.** ELN `POST /api/notes/batch` 엔드포인트가 있어야 한다.

**Files:**
- Modify: `services/file-service/src/lib/elnClient.ts`
- Modify: `services/file-service/src/processors/zipProcessor.ts`

> **Note:** file-service에는 Jest가 설정되어 있지 않다. tsc 빌드 확인으로 대체한다.

### 6-1: `postRequest` 헬퍼 추가

- [ ] `services/file-service/src/lib/elnClient.ts`에서 기존 `request<T>` 함수 블록 **아래**에 `postRequest<T>` 헬퍼를 추가한다.

```typescript
/** POST 요청 헬퍼 — 내부 서비스 전용 (x-user-id: system 헤더 포함) */
function postRequest<T>(path: string, body: unknown): Promise<T> {
  return new Promise((resolve, reject) => {
    const payload = JSON.stringify(body);
    const url = new URL(path, ELN_SERVICE_URL);
    const options = {
      hostname: url.hostname,
      port: parseInt(url.port || '8002'),
      path: url.pathname + url.search,
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Content-Length': Buffer.byteLength(payload),
        // x-internal-secret은 포함하지 않음: POST /api/notes/batch는 requireAuth 미들웨어만
        // 사용하며 requireAuth는 x-user-id 존재 여부만 확인한다. x-user-id/role/permissions
        // 세 헤더로 인증이 완료된다. 기존 GET 엔드포인트도 동일한 requireAuth를 사용하며,
        // x-internal-secret은 별도 미들웨어(requireInternalSecret)가 있는 경우에만 필요하다.
        'x-user-id': 'system',
        'x-user-role': 'admin',
        'x-user-permissions': JSON.stringify(['*']),
      },
      timeout: TIMEOUT_MS,
    };
    const req = http.request(options, (res) => {
      let data = '';
      res.on('data', (chunk) => { data += chunk; });
      res.on('end', () => {
        // 기존 request<T>와 동일하게 statusCode 먼저 확인 (proxy가 plain-text 404 반환 가능)
        if (res.statusCode === 404) {
          reject(new Error(`경로를 찾을 수 없습니다: ${path}`));
          return;
        }
        try {
          const parsed = JSON.parse(data);
          if (!parsed.ok) {
            reject(new Error(parsed.error || 'eln-service 오류'));
            return;
          }
          resolve(parsed.data as T);
        } catch {
          reject(new Error('eln-service 응답 파싱 실패'));
        }
      });
    });
    req.on('timeout', () => { req.destroy(); reject(new Error('eln-service 응답 시간 초과')); });
    req.on('error', (err) => reject(new Error(`eln-service 연결 실패: ${err.message}`)));
    req.write(payload);
    req.end();
  });
}
```

### 6-2: `getNotesBatch` 함수 추가

- [ ] 같은 파일 끝에 추가한다.

```typescript
/** 노트 ID 배열로 일괄 조회 — POST /api/notes/batch (N+1 HTTP 루프 대체) */
export async function getNotesBatch(ids: string[]): Promise<NoteExportData[]> {
  if (ids.length === 0) return [];
  return postRequest<NoteExportData[]>('/api/notes/batch', { ids });
}
```

### 6-3: `zipProcessor.ts` 루프 → 배치 교체

- [ ] `services/file-service/src/processors/zipProcessor.ts` 1번 줄 import에 `getNotesBatch` 추가:

```typescript
import { getNoteForExport, getSignedNotes, getNotesByTag, getNotesBatch } from '../lib/elnClient';
```

- [ ] 108–121번 줄의 루프 블록을 배치 호출로 교체한다.

기존:
```typescript
// 각 노트를 PDF로 변환 후 ZIP에 추가
(async () => {
  for (const noteId of noteIds) {
    try {
      const note = await getNoteForExport(noteId);
      const htmlContent = markdownToHtml(note.content);
      const pdfBuf = await generatePdf({ title: note.title, htmlContent });
      const safeName = note.title.replace(/[/\\?%*:|"<>]/g, '-').slice(0, 100);
      archive.append(pdfBuf, { name: `${safeName}.pdf` });
    } catch (err) {
      console.warn(`[zipProcessor] 노트 ${noteId} PDF 생성 실패, 건너뜀:`, err);
    }
  }
  archive.finalize();
})();
```

수정 후:
```typescript
// 노트 일괄 조회 후 PDF 변환 (N+1 HTTP 루프 → 단일 배치 요청)
// ELN 서비스가 없는 id를 조용히 생략하므로 기존 건너뜀 동작이 유지된다.
(async () => {
  let notes: Awaited<ReturnType<typeof getNotesBatch>>;
  try {
    notes = await getNotesBatch(noteIds);
  } catch (err) {
    console.warn('[zipProcessor] 노트 일괄 조회 실패:', err);
    notes = [];
  }
  for (const note of notes) {
    try {
      const htmlContent = markdownToHtml(note.content);
      const pdfBuf = await generatePdf({ title: note.title, htmlContent });
      const safeName = note.title.replace(/[/\\?%*:|"<>]/g, '-').slice(0, 100);
      archive.append(pdfBuf, { name: `${safeName}.pdf` });
    } catch (err) {
      console.warn(`[zipProcessor] 노트 ${note.id} PDF 생성 실패, 건너뜀:`, err);
    }
  }
  archive.finalize();
})();
```

### 6-4: 빌드 확인 및 커밋

- [ ] 컴파일 오류 없는지 확인한다.

```bash
cd services/file-service && npx tsc --noEmit
```

Expected: 오류 없음.

- [ ] 커밋한다.

```bash
git add services/file-service/src/lib/elnClient.ts \
        services/file-service/src/processors/zipProcessor.ts
git commit -m "fix(file-service): replace N HTTP loop with single batch call in zipProcessor"
```

---

## 최종 검증

### Jest 테스트 (자동)

- [ ] eln-service 전체 테스트 통과

```bash
cd services/eln-service && npm test
```

- [ ] signature-audit-service 전체 테스트 통과

```bash
cd services/signature-audit-service && npm test
```

### TypeScript 빌드 (자동)

- [ ] 전체 서비스 컴파일 오류 없음

```bash
cd /c/vscode/lab/lab-companion-feature-phase1-backend-infra
for svc in eln-service inventory-service signature-audit-service file-service api-gateway; do
  echo "=== $svc ===" && (cd services/$svc && npx tsc --noEmit) && echo "OK"
done
```

---

## 실행 순서 요약

```
Task 1 (ELN stats+batch 엔드포인트)  ← 선행 필수
  ├── Task 4 (signature-audit)       ← Task 1 이후
  ├── Task 5 (dashboard)             ← Task 1 이후
  └── Task 6 (file-service)          ← Task 1 이후

Task 2 (ELN 이중쿼리 수정)           ← 독립, 병렬 가능
Task 3 (inventory 수정)              ← 독립, 병렬 가능
```
